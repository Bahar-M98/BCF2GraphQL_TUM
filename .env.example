# Copy this file to .env to run against the public demo database out of the box:
#   cp .env.example .env
# .env itself is gitignored and will never be committed.
#
# To use your own data instead, replace this with any MongoDB connection string
# (e.g. a free MongoDB Atlas cluster) and import a BCF file with import_bcf.py.

MONGO_URI=mongodb+srv://Bahar:TUMCCBEProject@bcf2graphql.iudftom.mongodb.net/?appName=BCF2GraphQL
