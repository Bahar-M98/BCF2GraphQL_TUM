# Minimal runtime deps for the BCF REST + GraphQL API on Render.
# Excludes ifcopenshell, ifcdiff, bcf-client, streamlit, matplotlib,
# reportlab, and plotly — none are needed to serve BCF/MongoDB data.
ariadne>=0.29.0
fastapi>=0.135.3
motor>=3.7.1
python-dotenv>=1.2.2
uvicorn>=0.41.0
httpx>=0.28.1
